# Skill: Documentation

Generate documentation for APIs and modules.

## Guidelines

- Document all REST API endpoints with:
  - Method and path
  - Request parameters and body schema
  - Response schema
  - Error codes
- Document complex functions with a brief description, inputs, and outputs
- Keep documentation co-located with the code where possible
- Update documentation when the corresponding code changes
